{
  "name": "brand-llm-tracker",
  "version": "1.0.0",
  "description": "Brand LLM Tracker Monorepo",
  "scripts": {
    "install:frontend": "npm install --prefix apps/frontend",
    "install:backend": "npm install --prefix apps/backend",
    "install-all": "npm run install:frontend && npm run install:backend",
    "dev:frontend": "npm run dev --prefix apps/frontend",
    "dev:backend": "npm run start:dev --prefix apps/backend",
    "dev": "npx -y concurrently --kill-others \"npm run dev:frontend\" \"npm run dev:backend\""
  }
}
